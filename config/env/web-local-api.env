# Real Supabase credentials are inherited from the repository root .env file.
# Only the API endpoint is overridden for the local web + API development mode.
NUXT_PUBLIC_API_BASE=http://localhost:8787
